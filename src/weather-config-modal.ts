import { Modal, App } from "obsidian";

export class WeatherConfigModal extends Modal {
    constructor(app: App) {
        super(app);
    }

    onOpen(): void {
        // Stub: weather config modal
    }

    onClose(): void {
        // Stub
    }
}
